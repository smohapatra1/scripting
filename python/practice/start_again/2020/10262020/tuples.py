#Tuples 
t = ('a', 1, 'sam', 'a')
print (len(t))
#Find a word how many times repeated 
print (t.count('a'))
print (t.index('a')) # Only returns the values at first time 
#print ('Find duplicates {} '.format.'t.count('a')')

