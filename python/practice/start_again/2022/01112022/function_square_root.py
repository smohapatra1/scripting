#Define a funtion which takes an integer parameter 
#Function will return the squareroot of that parameter
import math
def square_root(a):
    b=math.sqrt(a)
    print (f'{b}')

square_root(int(input("Enter the number: ")))