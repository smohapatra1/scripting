#Define a function names add which takes two interger parameters 
# The function will return the sum of these parameters
import math
def add(a,b): 
    total= a + b
    print(f' Sum = {total}')
add(10,20)